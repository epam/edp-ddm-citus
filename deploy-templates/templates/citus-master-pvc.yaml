kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Values.master.volumes.claimName }}
spec:
  storageClassName: {{ .Values.storageClass }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.master.volumes.size }}