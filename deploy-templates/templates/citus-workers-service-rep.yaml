apiVersion: v1
kind: Service
metadata:
  annotations:
    service.alpha.kubernetes.io/tolerate-unready-endpoints: "true"
  name: {{ .Values.workersReplica.name }}
  labels:
    app: {{ .Values.workersReplica.name }}
spec:
  selector:
    app: {{ .Values.workersReplica.name }}
  clusterIP: None
  ports:
  - port: {{ .Values.workersReplica.containers.port }}
  publishNotReadyAddresses: true
